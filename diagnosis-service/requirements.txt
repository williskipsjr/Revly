fastapi>=0.115,<0.116
uvicorn[standard]>=0.32,<0.33
pydantic>=2.9,<3
# Phase 4: LLM classifier via the AgentRouter (Anthropic-compatible) gateway. Only this
# service ever receives the LLM key; it holds no payment credentials.
anthropic>=0.39,<1
